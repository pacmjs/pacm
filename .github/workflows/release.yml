name: Release

on:
    release:
        types: [created]

jobs:
    build-windows:
        runs-on: windows-latest

        steps:
        - name: Checkout repository
          uses: actions/checkout@v2

        - name: Set up Deno
          uses: denoland/setup-deno@v1
          with:
            deno-version: v2.x

        - name: Install dependencies
          run: deno install
  
        - name: Run package
          run: deno run package-windows

        - name: Rename output file
          run: mv release/pacm.exe release/pacm-windows.exe

        - name: Upload compiled files
          uses: actions/upload-artifact@v3
          with:
            name: dist-windows
            path: release/pacm-windows.exe

    build-linux:
        runs-on: ubuntu-latest

        steps:
        - name: Checkout repository
          uses: actions/checkout@v2

        - name: Set up Deno
          uses: denoland/setup-deno@v1
          with:
            deno-version: v2.x

        - name: Install dependencies
          run: deno install
  
        - name: Run package
          run: deno run package

        - name: Rename output file
          run: mv release/pacm release/pacm-linux

        - name: Upload compiled files
          uses: actions/upload-artifact@v3
          with:
            name: dist-linux
            path: release/pacm-linux

    build-macos:
        runs-on: macos-latest

        steps:
        - name: Checkout repository
          uses: actions/checkout@v2

        - name: Set up Deno
          uses: denoland/setup-deno@v1
          with:
            deno-version: v2.x

        - name: Install dependencies
          run: deno install

        - name: Run package
          run: deno run package

        - name: Rename output file
          run: mv release/pacm release/pacm-macos

        - name: Upload compiled files
          uses: actions/upload-artifact@v3
          with:
            name: dist-macos
            path: release/pacm-macos

    upload-release-assets:
        runs-on: ubuntu-latest
        needs: [build-windows, build-linux, build-macos]
        steps:
        - name: Download Windows artifact
          uses: actions/download-artifact@v3
          with:
            name: dist-windows
            path: ./dist-windows

        - name: Download Linux artifact
          uses: actions/download-artifact@v3
          with:
            name: dist-linux
            path: ./dist-linux

        - name: Download macOS artifact
          uses: actions/download-artifact@v3
          with:
            name: dist-macos
            path: ./dist-macos

        - name: Verify Windows artifact
          run: ls -la ./dist-windows

        - name: Verify Linux artifact
          run: ls -la ./dist-linux

        - name: Verify macOS artifact
          run: ls -la ./dist-macos

        - name: Upload Windows release asset
          uses: actions/upload-release-asset@v1
          env:
            GITHUB_TOKEN: ${{ secrets.PAT }}
          with:
            upload_url: ${{ github.event.release.upload_url }}
            asset_path: ./dist-windows/pacm-windows.exe
            asset_name: pacm-windows.exe
            asset_content_type: application/octet-stream

        - name: Upload Linux release asset
          uses: actions/upload-release-asset@v1
          env:
            GITHUB_TOKEN: ${{ secrets.PAT }}
          with:
            upload_url: ${{ github.event.release.upload_url }}
            asset_path: ./dist-linux/pacm-linux
            asset_name: pacm-linux
            asset_content_type: application/octet-stream

        - name: Upload macOS release asset
          uses: actions/upload-release-asset@v1
          env:
            GITHUB_TOKEN: ${{ secrets.PAT }}
          with:
            upload_url: ${{ github.event.release.upload_url }}
            asset_path: ./dist-macos/pacm-macos
            asset_name: pacm-macos
            asset_content_type: application/octet-stream